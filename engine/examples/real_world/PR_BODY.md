# Vendored `stb_image.h`: pick up upstream's `STBI_MAX_DIMENSIONS` hardening

*Generated by Mitos — a real parent→child patch applied via three-way merge, gated on an
independently-reviewed golden expected-postimage hash and checked through this repository's own build
and binary. Positional per-hunk placement is an experimental cross-check. Not auto-submitted.*

## Target & provenance

- **Upstream fix:** `https://github.com/nothings/stb` @ `d60594847ecca4553b18e7607d01328c58d95a42`
- **Upstream parent (from raw commit object):** `98ca24b8c7a69e1fc80e3d6a1e014b0e113980b8` (expected `98ca24b8c7a69e1fc80e3d6a1e014b0e113980b8`, match `True`)
- **This copy pinned at:** `712a47f946b98c30097eb1ada086ea00b18681ec`  ·  clone origin validated: `True`
- **Generator commit:** `0b0751a77ba5dbae7da2a87ba967cacea49084d6` (tree clean: `True`)  ·  **recipe digest:** `b836c206711d37f4`
- **Golden attestation:** attested=`True`, merged sha256 match=`True`, fix.diff match=`True` — the exact expected postimage is independently reviewed and pinned in the recipe; the run is compared to it (a hard gate). Generic positional verification beyond this golden-attested recipe is **experimental**.
- **Reproduce:** `python -m mitos repair` (two cold runs in different dirs reproduce every artifact byte-for-byte)

## Merge

`git diff 98ca24b8 d6059484 -- stb_image.h` applied via
`git merge-file --diff3`. **`merge_rc == 0`, 0 conflicts**
(clean); marker `STBI_MAX_DIMENSIONS`
0 → 21. Parsed hunks == patch hunks: `True`.

### Positional hunk cross-check (experimental — golden postimage is authoritative)

Alongside the golden gate, each hunk's target scope is the enclosing function of its coordinates in
the upstream child; inside that scope the copy is diffed against the merge and the change must be
exactly the hunk's additions and removals in one localized region, pinned to a surviving context
anchor (both sides for file-scope preprocessor context). This **rejects the covered regression cases**
(guard-after-return, second-of-two-identical-calls, wrong `#if` branch, obsolete-line-remaining) — it
is **not** a general proof that every mislocated/ambiguous mapping is caught. Acceptance rests on the
golden expected-postimage hash above.

| added code (sample) | nature | actual scope | Δ | pin | verified |
|---|---|---|---|---|---|
| `//  - If you define STBI_MAX_DIMENSIONS, stb` | preproc_or_comment | file-scope | +8/−0 | anchored | ✅ applied |
| `#ifndef STBI_MAX_DIMENSIONS` | preproc_or_comment | file-scope | +3/−0 | anchored | ✅ applied |
| `if (s->img_y > STBI_MAX_DIMENSIONS) return s` | statement | stbi__process_frame_head | +2/−0 | anchored | ✅ applied |
| `s->img_x = stbi__get32be(s);` | statement | stbi__parse_png_file | +4/−2 | anchored | ✅ applied |
| `if (s->img_y > STBI_MAX_DIMENSIONS) return s` | statement | stbi__bmp_load | +2/−0 | anchored | ✅ applied |
| `if (tga_height > STBI_MAX_DIMENSIONS) return` | statement | stbi__tga_load | +2/−0 | anchored | ✅ applied |
| `if (h > STBI_MAX_DIMENSIONS) return stbi__er` | statement | stbi__psd_load | +2/−0 | anchored | ✅ applied |
| `if (y > STBI_MAX_DIMENSIONS) return stbi__er` | statement | stbi__pic_load | +2/−0 | anchored | ✅ applied |
| `if (g->w > STBI_MAX_DIMENSIONS) return stbi_` | statement | stbi__gif_header | +2/−0 | anchored | ✅ applied |
| `if (height > STBI_MAX_DIMENSIONS) return stb` | statement | stbi__hdr_load | +2/−0 | anchored | ✅ applied |
| `if (s->img_y > STBI_MAX_DIMENSIONS) return s` | statement | stbi__pnm_load | +2/−0 | anchored | ✅ applied |

> clean three-way merge (merge_rc=0, 0 conflicts); acceptance rests on the golden expected-postimage hash gate; 11/11 upstream hunks also pass the experimental positional cross-check (rejects the covered regression cases, not a general mislocation proof).

## Coverage — structural vs behavioural (scoped)

- **Structurally merged loaders (9):** JPEG, PNG, BMP, TGA, PSD, PIC, GIF, HDR, PNM
- **Reachable loaders (5):** BMP, PNG, PSD, HDR, PNM
- **Behaviourally verified (2):** BMP, PNM
- **Invariant enforced:** `behavioural ⊆ reachable ⊆ modified`

PNG already rejected dimensions > 1<<24 before this fix (its own check), so the fix's newly-introduced default behavioural paths are BMP, PSD, HDR, PNM (4); of those, BMP and PNM are exercised here (2/4).

## Build + behaviour (constrained host execution)

- Baseline build: **ok** (fresh artifact `True`, sha256 `d8e0650d1f6cdd31`)
- Patched build: **ok** (fresh `True`, sha256 `24db7ab1dae81832`)

A rejection counts only on the exact allowed exit code **and** the expected diagnostic; signals,
crashes and timeouts always fail.

| behavioural probe | loader | expectation | result | detail |
|---|---|---|---|---|
| normal-image (4x4 BMP) | BMP | `identical` | ✅ pass | before rc=0 out='LmEx|s%BfL%N*FtKfNtQhCgJfQgJ'; after rc=0 out='LmEx|s%BfL%N*FtKfNtQhCgJfQgJ'; identical=True |
| oversized BMP (20000000x1) | BMP | `rejected_after_only` | ✅ pass | before rc=0 (accepted); after rc=1 diag=yes [ok] |
| oversized PNM (P6 20000000x1) | PNM | `rejected_after_only` | ✅ pass | before rc=0 (accepted); after rc=1 diag=yes [ok] |

## Verdict

**VERIFIED_SCOPED** — origin/parent validated, clean three-way merge matching the independently-reviewed golden postimage (the authoritative guarantee), every hunk also passing the experimental positional cross-check, both fresh builds green, probes met their exact exit code + diagnostic; behavioural coverage is a subset of the reachable loaders: behavioural coverage is scoped: 2/5 reachable loaders exercised; not exercised: PNG, PSD, HDR

<sub>constrained host execution: build/probe run their own process group with a scrubbed no-credentials env, CPU/file-size/core rlimits, wall timeouts (group killed on expiry) and bounded output; git runs from a from-scratch whitelisted env with isolated HOME into a fresh 0700 Mitos-owned validated clone. No network/PID/memory namespace isolation — internal allowlisted recipes only.</sub>
<sub>toolchain: Apple clang version 17.0.0 (clang-1700.6.4.2) · git version 2.41.0</sub>
